// First page, select a Team Manager

const inquirer = require("inquirer");
const questions = require("./questions")

// take user input from questions
inquirer
    prompt()
// When a manager is entered, it is stored in a variable and exported
// When completed, goto loop.js
